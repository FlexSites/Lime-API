exports.worker = async (db, conduit) => {
  conduit
    // // Specify the name of your service here
    // .name('CHANGEME')
    // // For services
    // .reaction('change.me.v1', (msg) => {

    // })
    // // For workers
    // .on('change.me.v1', (msg) => {

    // })
}
